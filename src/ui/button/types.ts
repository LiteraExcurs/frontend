export enum ButtonType {
  Filter = 'filter',
  Send = 'send',
  Subscribe = 'subscribe',
  Card =  'card',
  Request = 'request',
};

export enum Location {
  Capital = 'capital',
  Region = 'region',
}
